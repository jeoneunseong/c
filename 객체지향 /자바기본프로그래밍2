상수 선언
- 상수를 선언할면 앞에 final(파이널)을 선언해야함
- 선언 시 초기값 지정
- 실행 중 값 변경 불가 
final(상수 선언) int(데이터 타입) PRICE(상수 이름) = 10;(초기화)

 Scanner 클래스

Scanner a = new Scanner(System.in); //(생성)

include는 import랑 똑같음
int(데이터타입) i;(변수)는 int 타입으로 선언되었기 때문에 정수만 저장 가능함.
Scanner(클래스) a;(오브젝트, obj, 객체)는

논리연산자 &&은 참/거짓만 나옴
비트연산자인 a & b는 값이 나옴
